# Copy to env/prod.env (gitignored) and adjust.
LZ=gs
ENV=prod
NODE_ENV=production

# Base URL of the druthers-api backend on the shared homelab network.
API_BASE_URL=http://gs_druthers_api_prod:8000
