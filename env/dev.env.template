# Copy to env/dev.env (gitignored) and adjust.
LZ=m3
ENV=dev
NODE_ENV=production

# Base URL of the aleonard.us-api backend (server-side only; the BFF calls it).
# On the shared homelab network use the API container name, e.g.
# http://gs_phoenix_api_prod:8000
API_BASE_URL=http://m3_phoenix_api_dev:8000
