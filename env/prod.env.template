# Copy to env/prod.env (gitignored) and adjust.
LZ=gs
ENV=prod
NODE_ENV=production

# Base URL of the aleonard.us-api backend on the shared homelab network.
API_BASE_URL=http://gs_phoenix_api_prod:8000
